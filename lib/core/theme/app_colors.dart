// ============================================================
// APP COLORS — Paleta de colores de la aplicación
// ============================================================
// Centralizamos todos los colores aquí.
// Si quieres cambiar un color, solo lo cambias en UN lugar.

import 'package:flutter/material.dart';

class AppColors {
  // Constructor privado: esta clase no se instancia, solo se usa estáticamente
  AppColors._();

  static const Color background = Color(0xFF080B12);
  static const Color cardBackground = Color(0xFF121A2D);
  static const Color primary = Color(0xFF007BFF);
  static const Color accent = Color(0xFF00D2FF);
  static const Color textPrimary = Color(0xFFE0E6ED);
  static const Color textSecondary = Color(0xFF8A99AF);
}